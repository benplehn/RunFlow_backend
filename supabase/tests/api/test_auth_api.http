### 🔐 Signup (utilisateur jetable)
@NOW      = {{$timestamp}}
@EMAIL    = user{{NOW}}@example.com
@PASSWORD = MotdepasseFort123!

POST {{SUPABASE_API_URL}}/auth/v1/signup
Content-Type: application/json
apikey: {{SUPABASE_ANON_KEY}}
Accept: application/json

{
  "email": "{{EMAIL}}",
  "password": "{{PASSWORD}}"
}

@expect status == 200
@access_token = {{response.body.session.access_token}}

### ✅ Vérif accès protégé (GET /profiles)
GET {{SUPABASE_API_URL}}/rest/v1/profiles?select=id,username
apikey: {{SUPABASE_ANON_KEY}}
Authorization: Bearer {{access_token}}

@expect status == 200
